DJANGO_SECRET_KEY=
DATABASE_URL=
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=
TIME_ZONE=Asia/Tehran

# Telegram bot — get a fresh token from @BotFather (the leaked token must be revoked first)
TELEGRAM_BOT_TOKEN=
# The bot's @username (e.g. myclinicmedicationreminderbot) — do not include the @ prefix
TELEGRAM_BOT_USERNAME=

# How to run the Telegram bot (long-polling, dev only):
#   python manage.py run_telegram_bot
